# 🏗️ Arquitectura del Sistema - Diagrama Visual

## 📐 Arquitectura General

```
┌─────────────────────────────────────────────────────────────────┐
│                    MANTENIMIENTO INDUSTRIAL                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐         ┌──────────────────────────┐
│      FRONTEND             │         │      BACKEND             │
│   (Next.js + React)       │         │  (FastAPI + SQLAlchemy)  │
│   - PWA                   │         │  - JWT Auth              │
│   - Offline Mode          │         │  - CRUD Operations       │
│   - TypeScript            │◄──────►│  - QR Generation         │
│   - Tailwind CSS          │  HTTP   │  - Role-Based Access     │
│   - Components            │         │  - PostgreSQL            │
└──────────────────────────┘         └──────────────────────────┘
         │                                       │
         │                                       │
         └───────────────┬───────────────────────┘
                         │
                    ┌────▼─────┐
                    │ Database  │
                    │PostgreSQL │
                    └───────────┘
```

---

## 🔄 Flujo de Autenticación

```
┌─────────────┐
│   Usuario   │
└──────┬──────┘
       │ 1. Ingresa email/contraseña
       │
       ▼
┌──────────────────────┐
│ Frontend: Login Form  │
└──────┬───────────────┘
       │ 2. POST /api/auth/login
       │
       ▼
┌──────────────────────────────────┐
│ Backend: auth.py                 │
│ - Verificar email                │
│ - Verificar contraseña (bcrypt)  │
│ - Crear JWT token                │
└──────┬───────────────────────────┘
       │ 3. Retorna {token, usuario}
       │
       ▼
┌──────────────────────────────┐
│ Frontend: Almacenar token    │
│ - localStorage               │
│ - Headers Authorization      │
└──────┬──────────────────────┘
       │ 4. Usa token en futuras requests
       │
       ▼
┌──────────────────────────────┐
│ Backend: deps.py             │
│ - Verificar JWT              │
│ - Extraer usuario_id         │
│ - Validar rol                │
└──────┬──────────────────────┘
       │ 5. Retorna user info o error
       │
       ▼
┌──────────────────────────┐
│ Acceso a Ruta/Recurso    │
└──────────────────────────┘
```

---

## 🎯 Flujo de Crear Intervención

```
┌──────────────────┐
│   Técnico (App)  │
└────────┬─────────┘
         │ 1. Scanea QR de equipo
         │
         ▼
┌──────────────────────────────┐
│ Frontend: Scanner            │
│ - Lee QR (codigo_qr)         │
└────────┬─────────────────────┘
         │ 2. GET /api/equipos/qr/{codigo_qr}
         │
         ▼
┌──────────────────────────┐
│ Backend: crud.py         │
│ obtener_equipo_por_qr()  │
└────────┬─────────────────┘
         │ 3. Retorna Equipo
         │
         ▼
┌──────────────────────────────┐
│ Frontend: Interfaz Técnico   │
│ - Muestra datos equipo       │
│ - Selecciona tipo intervención
│ - Ingresa descripción        │
│ - Agrega observaciones       │
└────────┬─────────────────────┘
         │ 4. POST /api/intervenciones
         │    {equipo_id, tipo_id, descripción...}
         │
         ▼
┌──────────────────────────────┐
│ Backend: crud.py             │
│ crear_intervencion()         │
│ - Valida equipo existe       │
│ - Valida tipo existe         │
│ - Registra usuario_id        │
│ - Guarda en BD               │
└────────┬─────────────────────┘
         │ 5. Retorna Intervención creada
         │
         ▼
┌──────────────────────────────┐
│ Técnico completa trabajo     │
│ POST /api/intervenciones/{id}/completar
│ - Observaciones finales      │
│ - Tiempo utilizado           │
└────────┬─────────────────────┘
         │ 6. Intervención marcada completada
         │
         ▼
┌─────────────────────────────┐
│ BD: Registrada permanente   │
│ - Para historial            │
│ - Para reportes             │
│ - Para estadísticas         │
└─────────────────────────────┘
```

---

## 📊 Flujo de Datos - CRUD Equipos

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   CREATE    │     │    READ     │     │   UPDATE    │     │   DELETE    │
├─────────────┤     ├─────────────┤     ├─────────────┤     ├─────────────┤
│ POST        │     │ GET         │     │ PUT         │     │ DELETE      │
│ /equipos    │     │ /equipos    │     │ /equipos/{} │     │ /equipos/{} │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │                   │
       │ Validar           │ Buscar DB         │ Validar           │ Marcar
       │ Crear             │ Retornar          │ Actualizar        │ inactivo
       │ Guardar           │ JSON              │ Guardar           │ Guardar
       │                   │                   │                   │
       ▼                   ▼                   ▼                   ▼
    ┌─────────────────────────────────────────────────────┐
    │         Base de Datos: Tabla EQUIPOS                │
    ├─────────────────────────────────────────────────────┤
    │ id | codigo_qr | nombre | ubicacion | activo | ...  │
    └─────────────────────────────────────────────────────┘
```

---

## 🔐 Estructura de Seguridad por Nivel

```
┌──────────────────────────────────────────────────────────┐
│                   AUTENTICACIÓN (JWT)                    │
├──────────────────────────────────────────────────────────┤
│ Token = Header.Payload.Signature                         │
│ Payload: {user_id, email, rol, exp}                      │
└─────────────────────┬──────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        ▼             ▼             ▼
    ┌──────────┐  ┌──────────┐  ┌──────────┐
    │  ADMIN   │  │ TECNICO  │  │ LECTURA  │
    ├──────────┤  ├──────────┤  ├──────────┤
    │ GET      │  │ GET      │  │ GET      │
    │ POST     │  │ POST*    │  │ ✗        │
    │ PUT      │  │ PUT*     │  │ ✗        │
    │ DELETE   │  │ DELETE*  │  │ ✗        │
    └──────────┘  └──────────┘  └──────────┘
    
    * Solo recursos específicos
```

---

## 📈 Modelo de Datos Relacional

```
┌──────────────────────────────────────────────────────────────────┐
│                        USUARIOS (4)                              │
├──────────────────────────────────────────────────────────────────┤
│ PK: id                                                           │
│    email (UNIQUE) | nombre | hashed_password | rol | activo     │
│    fecha_creacion | fecha_actualizacion                          │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 1:N
                           │
      ┌────────────────────┴──────────────────────┐
      │                                           │
      ▼                                           ▼
┌─────────────────────────────────────────┐  ┌─────────────────┐
│      INTERVENCIONES (35+)               │  │   EQUIPOS (7)   │
├─────────────────────────────────────────┤  ├─────────────────┤
│ PK: id                                  │  │ PK: id          │
│ FK: usuario_id → USUARIOS               │  │ codigo_qr(UNIQ) │
│ FK: equipo_id → EQUIPOS                 │  │ nombre          │
│ FK: tipo_id → TIPOS_INTERVENCION        │  │ ubicacion       │
│ descripcion | observaciones             │  │ tipo            │
│ completada | fecha_inicio               │  │ modelo, serie   │
│ fecha_fin | tiempo_duracion             │  │ fabricante      │
│ fecha_creacion | fecha_actualizacion    │  │ activo          │
└──────┬───────────────────────────────────┘  └────────┬────────┘
       │ N                                       N      │
       │                                               │
       └──────────────────┬──────────────────────────┘
                          │ N
                          │
        ┌─────────────────▼─────────────────┐
        │  TIPOS_INTERVENCION (5)           │
        ├───────────────────────────────────┤
        │ PK: id                            │
        │    nombre (UNIQUE)                │
        │    descripcion                    │
        │    fecha_creacion                 │
        └───────────────────────────────────┘
```

---

## 🚀 Pipeline de Request-Response

```
HTTP Request
    │
    ▼
┌──────────────────────────┐
│ FastAPI Router           │
│ - Parsea URL             │
│ - Extrae parámetros      │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Dependencias (deps.py)   │
│ - Valida JWT             │
│ - Verifica rol           │
│ - Obtiene usuario        │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Controlador/Handler      │
│ - Valida inputs          │
│ - Llama CRUD             │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ CRUD (crud.py)           │
│ - Crea query SQLAlchemy  │
│ - Ejecuta en BD          │
│ - Retorna objeto ORM     │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ Validación Pydantic      │
│ - Serializa datos        │
│ - Convierte a JSON       │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│ HTTP Response            │
│ - Status code            │
│ - Headers                │
│ - JSON body              │
└──────────┬───────────────┘
           │
           ▼
        Cliente
```

---

## 📦 Stack Tecnológico

```
┌──────────────────────────────────────────┐
│           FRONTEND (Next.js)             │
├──────────────────────────────────────────┤
│ Next.js 13+      │ App Router            │
│ React 18         │ Hooks                 │
│ TypeScript       │ Type Safety           │
│ Tailwind CSS     │ Styling               │
│ PWA              │ Offline               │
│ Service Worker   │ Caching               │
└──────────────────────────────────────────┘
             │
       HTTP │ HTTPS
             │
┌──────────────────────────────────────────┐
│          BACKEND (FastAPI)               │
├──────────────────────────────────────────┤
│ FastAPI 0.104     │ Web Framework        │
│ Uvicorn           │ Server               │
│ SQLAlchemy 2.0    │ ORM                  │
│ Pydantic          │ Validation           │
│ PyJWT             │ Authentication       │
│ bcrypt            │ Password Hashing     │
│ python-jose       │ JWT Creation         │
│ qrcode            │ QR Generation        │
└──────────────────────────────────────────┘
             │
             │ SQL
             │
┌──────────────────────────────────────────┐
│         DATABASE (PostgreSQL)            │
├──────────────────────────────────────────┤
│ PostgreSQL 12+   │ Relational DB         │
│ Conexión Pool    │ Performance           │
│ Transactions     │ Consistency           │
│ Indexing         │ Fast Queries          │
└──────────────────────────────────────────┘
```

---

## 🔄 Ciclo de Vida de una Intervención

```
1. CREACIÓN
   Usuario (TECNICO) escanea QR
   ├─ GET /equipos/qr/{codigo_qr}
   ├─ POST /intervenciones
   └─ Estado: CREADA (completada=false)

2. EJECUCIÓN
   Técnico ejecuta mantenimiento
   ├─ PUT /intervenciones/{id}
   │  └─ Actualiza descripción/observaciones
   └─ Estado: EN_PROGRESO

3. FINALIZACIÓN
   Técnico completa intervención
   ├─ POST /intervenciones/{id}/completar
   │  ├─ fecha_fin = ahora
   │  ├─ completada = true
   │  └─ tiempo_duracion = calculado
   └─ Estado: COMPLETADA

4. REGISTRO
   Datos guardados permanentemente
   ├─ En historial de equipo
   ├─ En historial de técnico
   ├─ En estadísticas generales
   └─ Disponible para reportes

5. AUDITORÍA
   Revisión posterior (ADMIN)
   ├─ GET /equipos/{id}/historial
   ├─ GET /usuarios/{id}/intervenciones
   └─ GET /estadisticas
```

---

## 📊 Flujo de Información

```
Cliente (Browser/App)
    │
    ├─ Autenticación
    │  └─ POST /auth/login ──► JWT Token
    │
    ├─ Datos Maestros
    │  ├─ GET /equipos ──────► Lista equipos
    │  ├─ GET /tipos ────────► Tipos intervención
    │  └─ GET /usuarios ─────► Lista usuarios
    │
    ├─ Operaciones
    │  ├─ POST /intervenciones ──► Crear
    │  ├─ PUT /intervenciones ───► Actualizar
    │  ├─ DELETE /intervenciones ► Eliminar
    │  └─ POST /completar ───────► Finalizar
    │
    └─ Reportes
       ├─ GET /historial ───────► Por equipo
       ├─ GET /estadisticas ───► Totales
       └─ GET /reportes ───────► Avanzados

           │
           ▼
       
    Backend FastAPI
        │
        ├─ Validación (Pydantic)
        ├─ Autenticación (JWT)
        ├─ Autorización (Roles)
        ├─ Negocio (CRUD)
        └─ Persistencia (SQLAlchemy)
           │
           ▼
        
    PostgreSQL
        │
        ├─ Usuarios
        ├─ Equipos
        ├─ Intervenciones
        ├─ Tipos
        └─ Auditoría (futura)
```

---

**Esta arquitectura proporciona:**
- ✅ Separación clara de responsabilidades
- ✅ Seguridad en múltiples capas
- ✅ Escalabilidad horizontal
- ✅ Mantenibilidad a largo plazo
- ✅ Testing facilitado
